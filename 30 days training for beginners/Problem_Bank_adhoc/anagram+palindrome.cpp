// check if permutation is palindrome
#include<bits/stdc++.h>
using namespace std;

// check if a string is permutation palindrome

// check if anagram
// check if it is palindrome

bool checkanagram(string s1)
{
	sort(s1.begin(),s1.end());
	sort(s1.begin(),s2.end());
	
	return s1==s2;
}

bool checkpalindrome(string s1)
{
	string temp=s1;
	reverse(s1.begin(),s1.end());
	
	return s1==temp;
}

int main()
{
	
	coutcheck
	
}
