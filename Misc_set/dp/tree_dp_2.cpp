
// TREE COINS UM BUT NOT CHOOSE ADJACENT PESUDO CODE ONLE

//dp1(V)--> v is included cv + sumaation(max(dp2(u))

//dp2(V)--> v is not included max(dp1(u),dp2(u))

// return max(dp1(1),dp2(1))-->final ans

/////////////////////////////////////////////////////////////////
